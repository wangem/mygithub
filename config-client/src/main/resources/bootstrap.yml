# Spring.cloud.config.uri 必须在这个文件中 否则无法读取到
spring: 
  cloud:
    config: 
      name: didispace
      profile: test
      label: master
      uri: http://localhost:8080

# 关闭安全验证，否则无法刷新配置文件
management:
  security:
    enabled: false 